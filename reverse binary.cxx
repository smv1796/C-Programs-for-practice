#include<iostream>
#include<string>
#include<stack>
using namespace std;
int main()
{
    stack<int> st;
    st.push(9);
    cout<<st.size();
    st.push(7);
    cout<<endl<<st[0];

    unint32_t n;
}
